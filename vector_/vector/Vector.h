#include "pch.h"
#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

//www.geeksforgeeks.org/sorting-2d-vector-in-c-set-2-in-descending-order-by-row-and-column/
//Case 4 : To sort the entire 2D vector on basis of a particular column in descending order.
void sortEntireVectoerP();

bool sortcol(const vector<int>& v1,const vector<int>& v2 );

//First we take the input of row and and take the input of column of every row.
//Now the initialize the memory of every row by the size of column.
void inputOfrowAndTakeTh();


void vectoreraseandclearinC();

